/**
 * Общие паттерны для всех парсеров характеристик ТЗ (без привязки к одному формату).
 */

/** Служебный / юридический текст в ячейках ЕИС и похожих таблицах. */
export const PROC_CHAR_JUNK =
  /значени[ея]\s+характеристик[аи]?\s+не\s+может\s+изменя|не\s+может\s+изменяться\s+участник|участник(?:ом)?\s+закупк|участник\s+указывает|типов[аое]\s+решени|инструкци[яи]\s+по\s+заполнению|в\s+соответствии\s+со\s+ст[\.\d]|постановлени[емя]\s+правительства|федеральн[ыйого]\s+закон|обосновани[ея]\s+включен/i;

/**
 * Подпись/шапка в конце ТЗ (docx/pdf), иногда парсится как «графа: значение» в последнем блоке позиции.
 * См. `isDocumentTailLine` в parse-eis-wide-table (без циклического импорта parse-colon ↔ wide-table).
 */
export const CHARACTERISTIC_NAME_SIGNATORY_LINE_RE =
  /^начальник\s+(?:отдела|управлени|департамент|сектор)(?=\s|$)|^директор\s+(?:отдела|управлени|департамент|бюджет)(?=\s|$)|^руководитель\s+(?:отдела|управлени|организац)(?=\s|$)/iu;
